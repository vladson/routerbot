[package]
name = "routerbot-core"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0 OR MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
toml = "0.8"
thiserror = "1"
async-trait = "0.1"
tokio = { version = "1", features = ["sync"] }
