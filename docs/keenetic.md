# Keenetic

Keenetic is the first primary runtime target.

The default runtime path should work without Docker. Long polling is preferred over
webhooks, and dependencies should remain small enough for constrained router
environments.

Cross-compilation details will be added with the Keenetic adapter stage.
