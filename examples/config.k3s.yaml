# Example configuration for a k3s-backed media deployment.
bot:
  kind: telegram
  token_env: ROUTERBOT_TELEGRAM_TOKEN
  mode: long_polling

auth:
  allow_chats: []
  allow_users: []

adapters: {}
capabilities: {}
